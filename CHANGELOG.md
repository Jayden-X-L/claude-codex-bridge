# Changelog

## 0.1.0

- Added CLI-first Claude/Codex audit bridge.
- Added Codex.app bundled CLI fallback.
- Added durable `runs/<runId>/` evidence directories.
- Streamed child process stdout/stderr to files to avoid `spawnSync ENOBUFS`.
- Added `doctor` command for preflight diagnostics and install guidance.
- Added Claude skill and Codex plugin packaging.

